import { Routes } from '@angular/router';
import { ActorFormComponent } from './pages/actor-form/actor-form.component';

export const routes: Routes = [
  {
    path: 'actor',
    component: ActorFormComponent,
  },
];
